name: Deploy to VM

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run deploy.sh on VM
        run: bash ~/rndCRM/crm-of-r-d/deploy.sh
